# ホーム画面移植用プロンプト

以下の要件だけを実装してください。これは既存の防災アプリの「ホーム画面」部分を、別のシステムへ移植するための一回限りの実装指示です。

## 目的

住民が最初に開く防災情報ホーム画面を作成する。対象地域は青森市とする。画面は、現在の警報・注意報、行政からの避難指示、災害情報マップ、災害情報の凡例、避難所検索への導線を一つの画面にまとめる。

既存システムにすでにあるページ、ルーティング、認証、データ構造、デザインは変更しない。今回作るのはホーム画面と、ホーム画面が必要とする最小限のAPI・データ連携だけに限定する。

## 実装環境への適応

- まず対象プロジェクトの既存フレームワーク、ルーティング、CSS方針、コンポーネント構成を確認する。
- 既存の技術スタックとデザインシステムを優先し、不要なフレームワークや大規模な依存関係を追加しない。
- ホーム画面のルートは、そのシステムのトップページのルートにする。
- 既存の共通ヘッダーやレイアウトがある場合は再利用する。ただし、ホーム画面以外の表示や動作を壊さない。
- 実装後、既存の起動方法で画面が表示されることを確認する。

## 画面構成

### 共通レイアウト

- 日本語表示、レスポンシブ対応。
- ページ背景はごく薄い青みのあるグレー（例: `#f7f9fc`）。
- 上部に高さ約70pxの青いヘッダー（例: `#2458a8`）を置く。
- ヘッダー左側に白文字で「防災アプリ」を表示する。
- ヘッダー右側にハンバーガーメニューボタンを置く。クリックでメニューを開閉し、`aria-expanded`も更新する。
- メニューには少なくとも「ホーム」と「避難所検索」を表示する。移植先に既存の他ページがある場合も、それらをホーム画面のために変更・再設計しない。
- 本文は最大幅約1180px、中央寄せ、上下に余白を取る。
- 文字色は濃い青灰色、本文フォントは日本語が自然に表示されるものを使う。

### デスクトップ表示

本文は2カラムのダッシュボードにする。

- 左カラム: 幅約40%。警報・注意報、行政からの指示、避難所検索ボタン、凡例を縦に並べる。
- 右カラム: 幅約60%。「青森市の現在の状況マップ」を表示する地図カード。
- カラム間の余白は約24px。
- 各カードは白背景、角丸約18px、薄い影、内側余白約28px。
- 情報量が多くても詰まりすぎない、実用的な防災ダッシュボードにする。

### モバイル表示

画面幅600px以下では1カラムに変更する。

- 左カラムの内容を上から表示し、その後に地図を表示する。
- カードの内側余白は約18px、角丸は約12px。
- 地図の高さは約300pxにする。
- 見出しと自動更新表示がはみ出さないように折り返す。
- ボタン、メニュー、地図、カード内テキストを横スクロールなしで表示する。

## 各UIの仕様

### 1. 警報・注意報カード

見出しは「青森市の警報・注意報」。青系の枠線（約2px）を付ける。

初期状態では次を表示する。

> 現在、警報・注意報は発表されていません。

警報APIから情報を取得できた場合は、次の形式で表示する。

> 現在、{警報名}（{発表または継続}）、{警報名}（{発表または継続}）が発令中です

- 警報がない場合は青系または黒系の通常表示。
- 警報がある場合、高い危険度は赤（例: `#d52d2d`）、注意報のみは黄色・オレンジ（例: `#f9a825`）で表示する。
- APIエラー時は画面全体を壊さず、警報欄を空または取得できない状態として扱う。

### 2. 行政からの指示カード

見出しは「行政からの指示・該当地区」。警報カードと同じ青系の枠線を付ける。

- 住民向けで、解除・完了していない行政指示だけを一覧表示する。
- 各項目は指示内容を表示し、地区や対象避難所があれば `（該当地区: {名称}）` の形式で続ける。
- 指示がない場合は次を表示する。

> 現在発表されていません。

- 高い危険度は赤、注意報相当は黄色・オレンジで表示する。
- 指示文はバックエンドから渡される値として扱い、HTMLを無加工で挿入しない。

### 3. 避難所検索ボタン

左カラムに青い横幅いっぱいのボタンまたはリンクを置く。ラベルは「避難所を検索する」。

- 高さは押しやすいサイズにする。
- 白文字、青背景、角丸約8px。
- ホバー時は少し暗い青にする。
- 移植先に避難所検索機能がある場合はそのルートへリンクする。ルートがない場合は、将来接続できる明確な仮ルートを1つだけ用意する。
- このボタンのために避難所検索ページ自体を実装・変更しない。

### 4. 災害情報の凡例カード

見出しは「災害情報の凡例」。2列のグリッドで次の6項目を表示する。

1. 河川洪水
2. 土砂災害
3. 道路冠水
4. 道路寸断
5. 津波
6. 獣害

各項目には、地図上のピンと同じ色のピン型アイコンを右側に表示する。色は次の通りにする。

- 河川洪水: `#1565c0`
- 土砂災害: `#e65100`
- 道路冠水: `#f9a825`
- 道路寸断: `#c2185b`
- 津波: `#00838f`
- 獣害: `#6a1b9a`

SVGや既存アイコンライブラリが使える場合は、白い簡易記号入りのピン型アイコンを使う。アイコンにはアクセシブルなラベルを付ける。

### 5. 現在の状況マップ

見出しは「青森市の現在の状況マップ」。見出しの左に位置情報を示すピンアイコンを置く。

- Leafletが既存環境に適している場合はLeaflet 1.9.4相当を使う。
- OpenStreetMapのタイルを使い、必ず `© OpenStreetMap contributors` の帰属表示を出す。
- 初期中心座標は青森市付近の `[40.8244, 140.7400]`、初期ズームは `12`。
- 地図の通常時の高さは約500px、モバイルでは約300px。
- 警報・災害情報に位置情報がある場合、地図上に色付きのピンを表示する。
- ピンは円形の中心を持つ、白い縁取り付きのピン型アイコンにする。
- 危険種別とピンの色は凡例と一致させる。
- ピンをクリックすると、少なくとも以下をポップアップ表示する。
  - 災害名
  - 発生時刻
  - 地域名（青森市）
  - 現在の被害状況
  - 災害の規模
  - 今後の見通し
- 位置情報が不正、欠落、または座標が2要素でないデータは表示しない。
- 被災範囲の半径が与えられた場合は、同じ色の半透明円も表示する。
- ポップアップに表示する外部データはHTMLエスケープしてXSSを防ぐ。
- 警報が更新されたときは、古いピンと範囲を消してから新しいものを描画する。

## データ連携

### 警報API

ホーム画面から、次のようなJSON APIを呼び出す。実際のパスは移植先の規約に合わせてよいが、ホーム画面側で一貫して使うこと。

`GET /api/weather_warnings`

レスポンス例:

```json
{
  "area_name": "青森市",
  "warnings": [
    {
      "name": "洪水警報",
      "status": "発表",
      "severity": "high",
      "hazard_type": "river_flood",
      "location": [40.8244, 140.7400],
      "reported_at": "2026-07-14T18:38:00+09:00",
      "damage_status": "情報なし",
      "scale": "情報なし",
      "outlook": "情報なし",
      "coverage": {"radius": 1200}
    }
  ],
  "hazards": [],
  "severity": "high",
  "report_time": "2026年07月14日 18:38",
  "last_fetch_time": "2026年07月14日 18:40"
}
```

実装要件:

- 初回表示時に1回取得する。
- 10分（600000ミリ秒）ごとに自動更新する。
- 警報名とステータスをカードへ反映する。
- `warnings`または`hazards`の位置情報から地図ピンを描画する。
- 通信失敗時も地図や他のカードを維持する。
- コンポーネント破棄やページ離脱時にタイマー・イベントリスナーを適切に解除する。

### 行政指示データ

バックエンドからホーム画面へ次の形の配列を渡すか、同等のAPIから取得する。

```json
[
  {
    "content": "避難してください",
    "district": "対象地区",
    "area": "",
    "shelter": "対象避難所",
    "target": "住民",
    "status": "発令中"
  }
]
```

`target`が「住民」で、`status`が「解除」「完了」ではない項目だけを表示する。`district`、`area`、`shelter`は存在する最初の値を該当地区として表示する。

## アクセシビリティと安全性

- 見出し階層を正しく使う。
- カードには必要に応じて`aria-labelledby`を付ける。
- ボタン、リンク、メニューはキーボードで操作可能にする。
- アイコンだけの操作要素には`aria-label`を付ける。
- 色だけに依存せず、警報名や文字でも状態が分かるようにする。
- 外部APIや行政指示などの値は、画面に表示する前に適切にエスケープする。
- APIが空配列、欠損値、不正な座標を返しても例外で画面全体を停止させない。

## 完了条件

次の条件をすべて満たした状態で完了とする。

- トップページを開くと、上記のホーム画面が表示される。
- デスクトップで左側の情報カードと右側の地図が2カラムで表示される。
- スマートフォン幅で1カラムになり、横スクロールが発生しない。
- 警報なし、警報あり、APIエラーの3状態を確認できる。
- 行政指示の表示・非表示条件が機能する。
- 警報データの地図ピン、ポップアップ、被災範囲が機能する。
- 10分ごとの自動更新が設定されている。
- OpenStreetMapの帰属表示がある。
- ホーム画面以外のページ、認証、既存データ、既存ルートを不要に変更していない。
- 実装したファイル、起動方法、必要な環境変数やAPI設定を最後に簡潔に報告する。

上記以外の機能、ページ、管理画面、ログイン機能、避難所登録機能は追加しないでください。
