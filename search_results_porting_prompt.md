# 避難所検索結果画面移植用プロンプト

以下の要件を満たすように、既存のWebアプリに「避難所検索結果画面」とその関連機能を実装してください。

## 実装対象
- Flask + Jinja2 テンプレート
- Leaflet 地図表示
- 既存の避難所データ構造を利用

## 目的
- 避難所検索結果一覧を、表形式と地図形式の両方で表示する
- 複数の避難所を見やすく確認できるようにする
- 各避難所の状態を色と数値で直感的に把握できるようにする

## 画面要件
### 1. 検索結果ページのレイアウト
- `search_results.html` をベースにした画面を作成する
- 左側に検索結果の表、右側に地図を配置する
- レスポンシブ対応で、幅が狭い場合は縦並びにする

### 2. 表示する列と見た目
- 表には以下の列を表示する
  - 避難所名
  - 最大受け入れ可能人数
  - 現在の受け入れ人数
  - 残り人数
  - 空き率
- 各行の表示は1行で見やすくする
- 長い見出しは自然に2行で折り返すようにする
- 1行ごとに背景色を付ける
  - 空き率が30%以下: ピンク系
  - 空き率が31〜70%: 黄系
  - 空き率が71%以上: 緑系

### 3. 数値計算
- `capacity` と `current` から以下を算出する
  - `remaining = capacity - current`
  - `rate = (remaining / capacity) * 100`
- `capacity` が0または未定義の場合は `0` とする
- `remaining` はマイナスにならないように `max(capacity - current, 0)` を使う

### 4. 地図表示
- Leaflet を使って地図を表示する
- 各避難所の位置情報 (`lat`, `lng`) を使って地図上に表示する
- 地図上のマーカーはピン型にする
- 色は上記の空き率ルールに従って、以下の色を使用する
  - 30%以下: 赤
  - 31〜70%: 黄
  - 71%以上: 緑
- マーカーにホバーすると避難所名を表示する
- クリックすると、避難所名、受け入れ可能人数、現在の受け入れ人数、残り空き率をポップアップで表示する

### 5. 地図の初期表示
- 地図の初期中心座標は `[40.823, 140.745]` とする
- 初期ズームは `14` にする
- OSM の tile layer を使う

## 機能要件
- `search_results` 画面で避難所一覧を表示する
- `results` が存在する場合は検索結果一覧を表示し、存在しない場合は「該当する避難所が見つかりませんでした」と表示する
- `results` には各避難所の `name`, `capacity`, `current`, `lat`, `lng` が含まれている前提で実装する
- 既存のソート機能を維持する
  - `sort=remaining_desc` などの引数に対応できるようにする

## 実装時の注意事項
- 検索結果画面の見た目は、今の `search_results.html` の実装を踏襲する
- 既存のデザインやレイアウトを壊さないこと
- 既存の時刻表記やボタン配置はそのまま維持する
- 地図のマーカーは丸ではなくピン型にする
- ピン型のサイズは見やすい程度に少し大きめにする
- `white-space` と `word-break` の設定を使って、見出しが見づらくならないようにする

## 実装後の確認項目
以下を確認してください。
1. 表に避難所名・最大受け入れ可能人数・現在の受け入れ人数・残り人数・空き率が表示される
2. 3つの主要項目（避難所名・残り人数・空き率）は1行で見やすく表示される
3. 長い見出しがかぶらずに2行で見える
4. 地図上のマーカーがピン型で、色が空き率に応じて変わる
5. マーカーをクリックすると各避難所の詳細が表示される
6. ブラウザで地図が正しく表示される

## 出力仕様
- 実装結果をそのまま使える形で出力してください
- 既存コードを壊さず、必要な修正のみを行ってください
- もし依存関係が必要なら既存ライブラリの範囲で対応してください

## 最終状態のイメージ
- 左側に検索結果表
- 右側にピンマーカー付き地図
- 表と地図の両方が、避難所の空き状況を視覚的に確認できる
- 現在の実装で整えられた見た目を維持する
